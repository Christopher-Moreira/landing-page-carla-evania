import React from "react";

export default function Contact(){
    return(
        <div className="contact">
            <h1>Bem-vindo!</h1>
            <p>Esta é a página inicial feita em React.</p>
        </div>
    );
}